<% // Render individual movie details here %>
<% // This is how to do comments in EJS, by the way... %>

<h1><%= title %></h1>
<img src="<%= details.Poster %>" class="posterdetails" />
<p><%= rating %>/10</p>
<div class="details">
  <ul>
    <% for (const [x, y] of Object.entries(details)){ %>
    <%}; %>
  </ul>
</div>
<div class="faves">
  <form action="/faves" method="POST">
      <label for="query">Movie Title:</label>
      <input type="text" name="q" id="searchBar">
      <input type="submit" id='submit'>
  </form>
</div>